import 'package:firebase_auth_manager/src/widgets/phone_login_widget.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Test init of PhoneLoginWidget', (WidgetTester tester) async {
//    // Tell tester to build the widget
//    await tester.pumpWidget(PhoneLoginWidget(message: 'M',));
//    // for setState you need to call tester.pump(Duration duration)
//    // Search for widgets using finder
//    final body = find.text('M');
//    // Matcher and expectation
//    // additional matchers => findsNothing, findsWidgets, findNWidgets
//    expect(body, findsOneWidget);
  });
}
