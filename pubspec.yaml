name: firebase_auth_manager
description: A top level API to reduce boilerplate code regarding firebase auth.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase auth package
  firebase_auth: ^0.16.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:

